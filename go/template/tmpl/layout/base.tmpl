{{ define "base" }}
<html>
<head>
    <title>{{block "title" .}} {{end}}</title>
    {{block "style" .}} {{end}}
    {{block "js" .}} {{end}}
</head>
<body>
    <body>
        {{template "content" .}}
        <footer>{{block "footer" .}} {{end}}</footer>
    </body>
</body>
</html>

{{ end }}